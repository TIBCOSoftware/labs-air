kind: Pod
apiVersion: v1
metadata:
  name: dgraph-load-data-0
  labels:
    app: dgraph-loader
spec:
  volumes:
    - name: datadir
      persistentVolumeClaim:
        claimName: datadir-dgraph-server-0
  containers:
    - name: dgraph-load-data-0
      image: ubuntu
      volumeMounts:
      - name: datadir
        mountPath: /dgraph
      command: ["/bin/bash", "-ecx", "while :; do printf '.'; sleep 5 ; done"]
---
kind: Pod
apiVersion: v1
metadata:
  name: dgraph-load-data-1
  labels:
    app: dgraph-loader
spec:
  volumes:
    - name: datadir
      persistentVolumeClaim:
        claimName: datadir-dgraph-server-1
  containers:
    - name: dgraph-load-data-1
      image: ubuntu
      volumeMounts:
      - name: datadir
        mountPath: /dgraph
      command: ["/bin/bash", "-ecx", "while :; do printf '.'; sleep 5 ; done"]
---
kind: Pod
apiVersion: v1
metadata:
  name: dgraph-load-data-2
  labels:
    app: dgraph-loader
spec:
  volumes:
    - name: datadir
      persistentVolumeClaim:
        claimName: datadir-dgraph-server-2
  containers:
    - name: dgraph-load-data-2
      image: ubuntu
      volumeMounts:
      - name: datadir
        mountPath: /dgraph
      command: ["/bin/bash", "-ecx", "while :; do printf '.'; sleep 5 ; done"]

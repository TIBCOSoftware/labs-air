"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// Copyright (c) 2019-2019. TIBCO Software Inc. All Rights Reserved. Confidential & Proprietary.
const core_1 = require("@angular-devkit/core");
const schematics_1 = require("@angular-devkit/schematics");
// import { addDeclarationToModule } from '@schematics/angular/utility/ast-utils';
function ngGenDashboard(options) {
    return (_tree, context) => {
        const workspaceConfig = _tree.read('/angular.json');
        if (!workspaceConfig) {
            throw new schematics_1.SchematicsException('Could not find Angular workspace configuration');
        }
        // convert workspace to string
        const workspaceContent = workspaceConfig.toString();
        // parse workspace string into JSON object
        const workspace = JSON.parse(workspaceContent);
        if (!options.project) {
            options.project = workspace.defaultProject;
        }
        const projectName = options.project;
        const project = workspace.projects[projectName];
        const projectType = project.projectType === 'application' ? 'app' : 'lib';
        if (options.path === undefined) {
            options.path = `${project.sourceRoot}/${projectType}`;
        }
        context.logger.log('info', `Generate Component for dahboard '${core_1.strings.dasherize(options.name)}'`);
        const templateSource = schematics_1.apply(schematics_1.url('./files'), [
            schematics_1.applyTemplates({
                classify: core_1.strings.classify,
                dasherize: core_1.strings.dasherize,
                name: options.name
            }),
            schematics_1.move(core_1.normalize(options.path))
        ]);
        context.logger.log('info', `now add '${core_1.strings.dasherize(options.name)}Component' in 'declarations' of your @ngModule`);
        return schematics_1.chain([
            schematics_1.mergeWith(templateSource)
        ]);
    };
}
exports.ngGenDashboard = ngGenDashboard;
//# sourceMappingURL=index.js.map
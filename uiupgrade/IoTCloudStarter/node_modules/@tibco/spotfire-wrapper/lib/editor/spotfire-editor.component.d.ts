import { FormGroup, FormBuilder } from '@angular/forms';
import { Document as SpotDoc } from '../spotfire-webplayer';
import { PersistanceService } from '../persitence.service';
import { SpotfireViewerComponent } from '../viewer/spotfire-viewer.component';
import { DocumentService } from '../document.service';
export declare class SpotfireEditorComponent extends SpotfireViewerComponent {
    fb: FormBuilder;
    private storSvc;
    form: FormGroup;
    /**
      * When user hits Update button, depending on what settings are modified, call the right level of
      * method to update the dashboard
      */
    dataTables: {};
    pages: any[];
    possibleValues: string;
    custLabels: {
        showAbout: string;
        showAnalysisInformationTool: string;
        showAuthor: string;
        showClose: string;
        showCustomizableHeader: string;
        showDodPanel: string;
        showExportFile: string;
        showFilterPanel: string;
        showHelp: string;
        showLogout: string;
        showPageNavigation: string;
        showStatusBar: string;
        showToolBar: string;
        showUndoRedo: string;
    };
    constructor(fb: FormBuilder, storSvc: PersistanceService, d: DocumentService);
    update: (e: any) => void;
    doForm(doc: SpotDoc): void;
}

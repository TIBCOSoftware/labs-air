import { ToolbarButton } from '../models/tc-widget-header';
export declare class TcButtonsHelperService {
    constructor();
    createButton: (id: string, icon: string, filled: boolean, tooltip: string, visible: boolean, enabled: boolean) => ToolbarButton;
    updateButtons: (updatedToolbarButtons: ToolbarButton[], toolbarButtons: ToolbarButton[]) => ToolbarButton[];
    addButtons: (buttons: ToolbarButton[], toolbarButtons: ToolbarButton[]) => ToolbarButton[];
    removeButtons: (buttons: ToolbarButton[], toolbarButtons: ToolbarButton[]) => ToolbarButton[];
}

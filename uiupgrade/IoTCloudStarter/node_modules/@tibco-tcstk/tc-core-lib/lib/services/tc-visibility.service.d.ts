import { ElementRef } from '@angular/core';
import { Observable } from 'rxjs';
export declare class TcVisibilityService {
    private pageVisible$;
    constructor(document: any);
    elementInSight(element: ElementRef): Observable<boolean>;
}

/**
 * @ngdoc component
 * @name LegacyIframeService
 *
 * @description
 *
 * Used to handle synchronization and loading of legacy form iframe
 *
 * Will emit false until the iFrame has posted it's ready message
 *
 *
 */
export declare class LegacyIframeService {
    private iWorkitemFrameReady;
    private iProcessFrameReady;
    workitemStatus: import("rxjs").Observable<boolean>;
    processStatus: import("rxjs").Observable<boolean>;
    constructor();
    private receiveMessage;
}

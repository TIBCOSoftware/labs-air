import { Deserializable } from './deserializable';
export declare class ToolbarButton implements Deserializable {
    id: string;
    icon: string;
    visible: boolean;
    enabled: boolean;
    tooltip: string;
    filled: boolean;
    deserialize(input: any): this;
}

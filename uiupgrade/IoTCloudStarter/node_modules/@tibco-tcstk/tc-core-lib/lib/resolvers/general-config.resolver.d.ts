export declare const DEFAULT_ADMIN_GROUP: RoleAttribute;
import { Resolve, Router } from '@angular/router';
import { Observable } from 'rxjs';
import { TcSharedStateService } from '../services/tc-shared-state.service';
import { HttpClient } from '@angular/common/http';
import { TcGeneralConfigService } from '../services/tc-general-config.service';
import { GeneralConfig, RoleAttribute } from '../models/tc-general-config';
import { Location } from '@angular/common';
export declare class GeneralConfigResolver implements Resolve<Observable<GeneralConfig>> {
    private tcSharedState;
    private generalConfigService;
    private http;
    private location;
    private router;
    DEFAULT_CONFIG_URL: string;
    APP_ID_URL: string;
    private sandboxId;
    defaultAppConfig: GeneralConfig;
    private uiAppId;
    constructor(tcSharedState: TcSharedStateService, generalConfigService: TcGeneralConfigService, http: HttpClient, location: Location, router: Router);
    setSandbox: (sandboxId: number) => void;
    private getDefaultAppConfig;
    private getAppId;
    resolve(): Observable<GeneralConfig>;
}

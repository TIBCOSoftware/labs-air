import { OnChanges, OnInit } from '@angular/core';
import { MatPaginator, MatSort } from '@angular/material';
import { TibcoCloudTableDataSource } from './tibco-cloud-table-datasource';
export declare class TibcoCloudTableComponent implements OnInit, OnChanges {
    jsonSource: string;
    paginator: MatPaginator;
    sort: MatSort;
    dataSource: TibcoCloudTableDataSource;
    id: string;
    /** Columns displayed in the table. Columns IDs can be added, removed, or reordered. */
    displayedColumns: any[];
    columns: any[];
    ngOnInit(): void;
    ngOnChanges(changes: any): void;
    updateTable(): void;
    camelCaseTW(header: string | any): any;
}

import { BehaviorSubject, Observable } from 'rxjs';
export declare class MessageTopicService {
    topics: Array<{
        name: string;
        subject: BehaviorSubject<any>;
    }>;
    constructor();
    sendMessage(name: string, message: string): void;
    clearMessages(name: string): void;
    getMessage(name: string): Observable<any>;
    createTopicIfNotExists(name: string, init: any): void;
}

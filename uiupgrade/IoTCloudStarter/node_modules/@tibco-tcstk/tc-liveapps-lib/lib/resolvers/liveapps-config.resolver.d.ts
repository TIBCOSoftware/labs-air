import { ActivatedRouteSnapshot, Resolve } from '@angular/router';
import { Observable } from 'rxjs';
import { TcSharedStateService } from '@tibco-tcstk/tc-core-lib';
import { HttpClient } from '@angular/common/http';
import { TcLiveAppsConfigService } from '../services/tc-live-apps-config.service';
import { LiveAppsConfig } from '../models/tc-liveapps-config';
import { TcCaseCardConfigService } from '../services/tc-case-card-config.service';
import { Location } from '@angular/common';
export declare class LiveAppsConfigResolver implements Resolve<Observable<LiveAppsConfig>> {
    private tcSharedState;
    private liveAppsConfigService;
    private caseCardConfigService;
    private http;
    private location;
    DEFAULT_CONFIG_URL: string;
    APP_ID_URL: string;
    private sandboxId;
    defaultAppConfig: LiveAppsConfig;
    private uiAppId;
    constructor(tcSharedState: TcSharedStateService, liveAppsConfigService: TcLiveAppsConfigService, caseCardConfigService: TcCaseCardConfigService, http: HttpClient, location: Location);
    setSandbox: (sandboxId: number) => void;
    private getDefaultAppConfig;
    private getAppId;
    private triggerCardConfigFetch;
    resolve(routeSnapshot: ActivatedRouteSnapshot): Observable<LiveAppsConfig>;
}

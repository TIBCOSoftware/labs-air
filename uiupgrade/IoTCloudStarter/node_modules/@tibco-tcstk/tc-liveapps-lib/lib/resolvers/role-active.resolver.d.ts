import { Resolve, Router } from '@angular/router';
import { Observable } from 'rxjs';
import { LiveAppsService } from '../services/live-apps.service';
import { RoleAttribute, TcGeneralConfigService, TcSharedStateService } from '@tibco-tcstk/tc-core-lib';
import { HttpClient } from '@angular/common/http';
import { Location } from '@angular/common';
import { TcRolesService } from '../services/tc-roles-service.ts.service';
export declare class RoleActiveResolver implements Resolve<Observable<RoleAttribute>> {
    private rolesService;
    private liveAppsService;
    private tcSharedStateService;
    private generalConfigService;
    private httpClient;
    private location;
    private router;
    constructor(rolesService: TcRolesService, liveAppsService: LiveAppsService, tcSharedStateService: TcSharedStateService, generalConfigService: TcGeneralConfigService, httpClient: HttpClient, location: Location, router: Router);
    resolve(): Observable<RoleAttribute>;
}

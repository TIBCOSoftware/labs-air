import { Resolve, ActivatedRouteSnapshot } from '@angular/router';
import { Observable } from 'rxjs';
import { LiveAppsService } from '../services/live-apps.service';
import { CaseInfo } from '../models/liveappsdata';
export declare class CaseDataResolver implements Resolve<Observable<CaseInfo>> {
    private liveapps;
    constructor(liveapps: LiveAppsService);
    resolve(routeSnapshot: ActivatedRouteSnapshot): Observable<CaseInfo>;
}

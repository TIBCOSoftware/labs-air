import { Observable } from 'rxjs';
import { HttpClient } from '@angular/common/http';
import { ApiResponseText } from '../models/liveappsdata';
import { LiveAppsService } from './live-apps.service';
import { DocumentList, OrgFolder } from '../models/tc-document';
export declare class TcDocumentService {
    private http;
    private liveapps;
    constructor(http: HttpClient, liveapps: LiveAppsService);
    createOrgFolder(name: string): Observable<ApiResponseText>;
    getOrgFolder(name: string, useCache: boolean, flushCache: boolean): Observable<OrgFolder>;
    initOrgFolder(name: string): Observable<ApiResponseText>;
    listDocuments(folderType: string, folderId: string, sandboxId: number, filter: string): Observable<DocumentList>;
    private parseDocument;
    deleteDocument(folderType: string, folderId: string, documentName: string, sandboxId: number): Observable<ApiResponseText>;
    getUrlForDocument(folderType: string, folderId: string, docId: string, docVersion: string, sandboxId: number): string;
    downloadDocument(folderType: string, folderId: string, docId: string, docVersion: string, sandboxId: number): Observable<any>;
    uploadDocument(folderType: string, folderId: string, sandboxId: number, fileToUpload: File, fileName: string, description: string): Observable<any>;
    private getIcon;
}

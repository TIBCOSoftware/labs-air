import { OnInit, EventEmitter } from '@angular/core';
import { LandingPageItemConfig, TcGeneralLandingPageService, ActionButtonConfig, RouteAction } from '@tibco-tcstk/tc-core-lib';
import { Router } from '@angular/router';
/**
 * Landing page
 *
 * This component allows a configurable landing page on a per role basis
 *
 * @example <tcla-live-apps-landing-page></tcla-live-apps-landing-page>
 */
export declare class LiveAppsLandingPageComponent implements OnInit {
    private router;
    private landingPageService;
    uiAppId: string;
    roleId: string;
    getStartedEvent: EventEmitter<RouteAction>;
    title: string;
    subtitle: string;
    backgroundImage: string;
    topMargin: string;
    highlights: LandingPageItemConfig[];
    actionButtons: ActionButtonConfig[];
    navigateURL: string;
    constructor(router: Router, landingPageService: TcGeneralLandingPageService);
    ngOnInit(): void;
    getStartedClick: (route: RouteAction) => void;
}

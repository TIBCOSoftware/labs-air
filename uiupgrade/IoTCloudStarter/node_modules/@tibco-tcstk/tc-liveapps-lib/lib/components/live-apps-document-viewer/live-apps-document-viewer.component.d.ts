import { OnInit } from '@angular/core';
import { DomSanitizer, SafeResourceUrl } from '@angular/platform-browser';
import { Document } from '../../models/tc-document';
import { TcDocumentService } from '../../services/tc-document.service';
import { Location } from '@angular/common';
/**
 * Display document
 *
 *@example <tcla-live-apps-document-viewer></tcla-live-apps-document-viewer>
 */
export declare class LiveAppsDocumentViewerComponent implements OnInit {
    protected sanitizer: DomSanitizer;
    protected documentsService: TcDocumentService;
    protected location: Location;
    /**
     * Document metadata object from API - describes the document to display
     */
    document: Document;
    /**
     * orgFolders' or 'caseFolders' - different API calls made according to which one this is
     */
    folderType: string;
    /**
     * The organisation folder to store/retrieve documents
     */
    folderId: string;
    /**
     * sandboxId - this comes from claims resolver
     */
    sandboxId: number;
    sanitizedResourceUrl: SafeResourceUrl;
    constructor(sanitizer: DomSanitizer, documentsService: TcDocumentService, location: Location);
    ngOnInit(): void;
}

import { OnChanges, OnInit, SimpleChanges } from '@angular/core';
import { LiveAppsCaseCreatorComponent } from '../live-apps-case-creator/live-apps-case-creator.component';
import { LiveAppsService } from '../../services/live-apps.service';
import { LiveAppsLegacyProcessComponent } from '../live-apps-legacy-process/live-apps-legacy-process.component';
/**
 * Wraps rendering an execution of an action
 *
 *@example <tcla-live-apps-case-action></tcla-live-apps-case-action>
 */
export declare class LiveAppsCaseActionComponent extends LiveAppsCaseCreatorComponent implements OnInit, OnChanges {
    protected lasvc: LiveAppsService;
    /**
     * The case reference
     */
    caseRef: string;
    /**
     * Whether to show the header bar in the widget - eg. favorites on home page (contains icon etc) - if off icons still appear without bar
     */
    showHeader: boolean;
    /**
     * Enable legacy actions
     */
    legacyActions: boolean;
    /**
     * Allow override of forms framework
     * Options: bootstrap-4 or material-design
     */
    formsFramework: string;
    legacyProcessComponent: LiveAppsLegacyProcessComponent;
    originalData: any;
    caseState: string;
    private getMainCaseTypeFromSchema;
    private getCaseData;
    cancelAction: () => void;
    constructor(lasvc: LiveAppsService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
}

import { MatDialogRef } from '@angular/material';
import { CaseCreatorSelectionContext } from '../../models/tc-case-creator';
import { CaseType, ProcessId } from '../../models/liveappsdata';
import { CustomFormDefs } from '@tibco-tcstk/tc-forms-lib';
/**
 * Dialog used on home screen to allow case creation (wraps other components)
 *
 *@example <tcla-live-apps-creator-dialog></tcla-live-apps-creator-dialog>
 */
export declare class LiveAppsCreatorDialogComponent {
    dialogRef: MatDialogRef<LiveAppsCreatorDialogComponent>;
    data: CaseCreatorSelectionContext;
    application: CaseType;
    sandboxId: number;
    initialData: any;
    createdCase: ProcessId;
    customFormDefs: CustomFormDefs;
    legacyCreators: boolean;
    formsFramework: string;
    constructor(dialogRef: MatDialogRef<LiveAppsCreatorDialogComponent>, data: CaseCreatorSelectionContext);
    handleCaseCreated: (createdCase: ProcessId) => void;
    openCase: () => void;
    closeDialog: () => void;
}

import { OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { Roles } from '../../models/tc-groups-data';
import { TcAccessControlService } from '../../services/tc-access-control.service';
import { MatSnackBar } from '@angular/material';
export declare class LiveAppsSettingsAccessControlComponent implements OnInit {
    private route;
    private accessControlService;
    private snackBar;
    displayedColumns: string[];
    dataSource: any[];
    private accessControlConfiguration;
    allRoles: Roles;
    isGroup(index: any, item: any): boolean;
    constructor(route: ActivatedRoute, accessControlService: TcAccessControlService, snackBar: MatSnackBar);
    ngOnInit(): void;
    runSaveFunction(): void;
    private generateTables;
    isSelected: (row: any, role: any) => boolean;
    toggle: (row: any, role: string) => void;
}

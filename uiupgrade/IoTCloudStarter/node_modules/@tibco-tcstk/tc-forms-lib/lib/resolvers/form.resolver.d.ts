import { Resolve } from '@angular/router';
import { Observable } from 'rxjs';
import { HttpClient } from '@angular/common/http';
import { Location } from '@angular/common';
import { CustomFormDefs } from '../models/tc-custom-forms';
export declare class FormResolver implements Resolve<Observable<CustomFormDefs>> {
    private http;
    private location;
    CUSTOM_FORMS_CONFIG_URL: string;
    constructor(http: HttpClient, location: Location);
    resolve(): Observable<CustomFormDefs>;
}

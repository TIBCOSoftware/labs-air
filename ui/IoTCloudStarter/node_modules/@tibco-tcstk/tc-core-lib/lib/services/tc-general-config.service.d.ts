import { Observable } from 'rxjs';
import { Location } from '@angular/common';
import { GeneralConfig } from '../models/tc-general-config';
import { TcSharedStateService } from './tc-shared-state.service';
export declare class TcGeneralConfigService {
    private location;
    private sharedStateService;
    constructor(location: Location, sharedStateService: TcSharedStateService);
    createGeneralConfig(sandboxId: number, uiAppId: string, generalConfig: GeneralConfig): Observable<string>;
    getGeneralConfig(uiAppId: string, useCache: boolean, flushCache: boolean): Observable<GeneralConfig>;
    updateGeneralConfig(sandboxId: number, uiAppId: string, generalConfig: GeneralConfig, id: string): Observable<GeneralConfig>;
}

import { PipeTransform } from '@angular/core';
import { SafeHtml } from '@angular/platform-browser';
export declare class HighlightPipe implements PipeTransform {
    static SINGLE_MATCH: string;
    static SINGLE_AND_STARTS_WITH_MATCH: string;
    static MULTI_MATCH: string;
    transform(data: string, highlightText: string, option?: string, caseSensitive?: boolean, highlightStyleName?: string): SafeHtml;
}

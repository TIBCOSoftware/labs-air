/**
 * @ngdoc component
 * @name mockingInterceptor
 *
 * @description
 * This interceptor attempts to use local json files from the assets directory to mock Tibco Cloud API calls
 *
 * Each mock file can contain either a single response or a "mockedResponses" object that contains specific URLs and responses
 *
 * @usage
 *
 *
 *
 */
import { HttpEvent, HttpRequest, HttpInterceptor, HttpHandler, HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
export declare class MockingInterceptor implements HttpInterceptor {
    private http;
    MOCK_BASE_PATH: string;
    constructor(http: HttpClient);
    readMockJSON(url: any): Observable<any>;
    intercept(req: HttpRequest<any>, next: HttpHandler): Observable<HttpEvent<any>>;
    sendRequest: (req: HttpRequest<any>, next: HttpHandler) => Observable<HttpEvent<any>>;
}

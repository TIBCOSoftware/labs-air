import { Observable, Subject } from 'rxjs';
export declare class MessageQueueService {
    queues: Array<{
        name: string;
        subject: Subject<any>;
    }>;
    constructor();
    sendMessage(name: string, message: string): void;
    clearMessages(name: string): void;
    getMessage(name: string): Observable<any>;
    createQueueIfNotExists(name: string): void;
}

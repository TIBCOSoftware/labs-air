import { ActivatedRouteSnapshot, Resolve } from '@angular/router';
import { Observable } from 'rxjs';
import { HttpClient } from '@angular/common/http';
import { ConfigurationMenuConfig } from '../models/tc-configuration-menu-config';
import { Location } from '@angular/common';
export declare class ConfigurationMenuConfigResolver implements Resolve<Observable<ConfigurationMenuConfig>> {
    private http;
    private location;
    DEFAULT_CONFIG_URL: string;
    APP_ID_URL: string;
    constructor(http: HttpClient, location: Location);
    private getConfigurationMenuConfig;
    private getAppId;
    resolve(routeSnapshot: ActivatedRouteSnapshot): Observable<ConfigurationMenuConfig>;
}

import { ModuleWithProviders } from '@angular/core';
import { DomSanitizer } from '@angular/platform-browser';
import { MatIconRegistry } from '@angular/material';
import { Router } from '@angular/router';
import { Location } from '@angular/common';
import { MessageTopicService } from './common/tc-core-topic-comm';
export declare class TcCoreLibModule {
    private matIconRegistry;
    private domSanitizer;
    private location;
    private router;
    private messageService;
    private ms;
    static forRoot(): ModuleWithProviders;
    constructor(matIconRegistry: MatIconRegistry, domSanitizer: DomSanitizer, location: Location, router: Router, messageService: MessageTopicService);
}

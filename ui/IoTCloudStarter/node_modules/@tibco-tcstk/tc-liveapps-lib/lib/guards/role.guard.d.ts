import { Router, CanActivate, ActivatedRouteSnapshot, RouterStateSnapshot } from '@angular/router';
import { LiveAppsService } from '../services/live-apps.service';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { TcGeneralConfigService, TcSharedStateService } from '@tibco-tcstk/tc-core-lib';
import { Location } from '@angular/common';
export declare class RoleGuard implements CanActivate {
    private liveapps;
    private router;
    private http;
    private location;
    private sharedStateService;
    private generalConfigService;
    DEFAULT_CONFIG_URL: string;
    constructor(liveapps: LiveAppsService, router: Router, http: HttpClient, location: Location, sharedStateService: TcSharedStateService, generalConfigService: TcGeneralConfigService);
    private getRouteAccessControlConfig;
    private getRouteDef;
    private hasAccess;
    canActivate(route: ActivatedRouteSnapshot, state: RouterStateSnapshot): Observable<boolean>;
}

import { CanActivate, ActivatedRouteSnapshot, RouterStateSnapshot, ActivatedRoute, Router } from '@angular/router';
import { LiveAppsService } from '../services/live-apps.service';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { TcGeneralConfigService, TcSharedStateService } from '@tibco-tcstk/tc-core-lib';
import { Location } from '@angular/common';
import { TcRolesService } from '../services/tc-roles-service.ts.service';
import { TcAccessControlService } from '../services/tc-access-control.service';
export declare class RoleGuard implements CanActivate {
    private liveapps;
    private rolesService;
    private http;
    private location;
    private route;
    private sharedStateService;
    private generalConfigService;
    private accessControlService;
    private router;
    DEFAULT_CONFIG_URL: string;
    constructor(liveapps: LiveAppsService, rolesService: TcRolesService, http: HttpClient, location: Location, route: ActivatedRoute, sharedStateService: TcSharedStateService, generalConfigService: TcGeneralConfigService, accessControlService: TcAccessControlService, router: Router);
    canActivate(route: ActivatedRouteSnapshot, state: RouterStateSnapshot): Observable<boolean>;
}

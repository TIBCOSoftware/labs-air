import { ActivatedRouteSnapshot, Resolve, Router } from '@angular/router';
import { Observable } from 'rxjs';
import { LiveAppsService } from '../services/live-apps.service';
import { TcGeneralConfigService, TcSharedStateService } from '@tibco-tcstk/tc-core-lib';
import { Roles } from '../models/tc-groups-data';
import { HttpClient } from '@angular/common/http';
import { Location } from '@angular/common';
export declare class AllRolesResolver implements Resolve<Observable<Roles>> {
    private sharedStateService;
    private generalConfigService;
    private http;
    private liveapps;
    private location;
    private router;
    constructor(sharedStateService: TcSharedStateService, generalConfigService: TcGeneralConfigService, http: HttpClient, liveapps: LiveAppsService, location: Location, router: Router);
    resolve(routeSnapshot: ActivatedRouteSnapshot): Observable<Roles>;
}

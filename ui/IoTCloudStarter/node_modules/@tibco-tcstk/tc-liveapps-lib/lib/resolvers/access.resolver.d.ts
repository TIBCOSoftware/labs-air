import { Resolve } from '@angular/router';
import { Observable } from 'rxjs';
import { RouteAccessControlConfig } from '../models/tc-groups-data';
import { HttpClient } from '@angular/common/http';
import { Location } from '@angular/common';
export declare class AccessResolver implements Resolve<Observable<RouteAccessControlConfig>> {
    private http;
    private location;
    ACCESS_CONTROL_CONFIG_URL: string;
    constructor(http: HttpClient, location: Location);
    resolve(): Observable<RouteAccessControlConfig>;
}

import { CaseType } from './liveappsdata';
import { CustomFormDefs } from '@tibco-tcstk/tc-forms-lib';
export declare class CaseCreatorSelectionContext {
    application: CaseType;
    initialData: any;
    sandboxId: number;
    customFormDefs: CustomFormDefs;
    legacyCreators: boolean;
    formsFramework: string;
    constructor(application: CaseType, initialData: any, sandboxId: number, customFormDefs: CustomFormDefs, legacyCreators: boolean, formsFramework: string);
}

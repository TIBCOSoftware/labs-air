import { OnChanges, OnInit, SimpleChanges } from '@angular/core';
import { DomSanitizer, SafeHtml } from '@angular/platform-browser';
import { HttpClient } from '@angular/common/http';
import { LiveAppsService } from '../../services/live-apps.service';
import { Location } from '@angular/common';
import { LiveAppsComponent } from '../live-apps-component/live-apps-component.component';
/**
 * renders a single icon for a state
 *
 * ![alt-text](../live-apps-state-icon.png "")
 *
 *@example <tcla-live-apps-state-icon></tcla-live-apps-state-icon>
 */
export declare class LiveAppsStateIconComponent extends LiveAppsComponent implements OnInit, OnChanges {
    private sanitizer;
    private http;
    private liveapps;
    private location;
    id: string;
    iconPath: string;
    color: string;
    iconHostURL: string;
    appId: string;
    iconSVG: SafeHtml;
    svgcontents: string;
    constructor(sanitizer: DomSanitizer, http: HttpClient, liveapps: LiveAppsService, location: Location);
    refillSVG: (fill: any) => void;
    refresh: (icon: any, fill: any) => void;
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
}

import { EventEmitter, OnInit } from '@angular/core';
import { JsonSchema, Metadata } from '../../models/liveappsdata';
import { ReplaySubject } from 'rxjs';
import { LiveAppsComponent } from '../live-apps-component/live-apps-component.component';
import { TcCaseDataService } from '../../services/tc-case-data.service';
import { CustomFormDefs } from '@tibco-tcstk/tc-forms-lib';
/**
 * Displays data for a case in a widget (high level)
 *
 * ![alt-text](../live-apps-case-data.png "Image")
 *
 *@example <tcla-live-apps-case-data></tcla-live-apps-case-data>
 */
export declare class LiveAppsCaseDataComponent extends LiveAppsComponent implements OnInit {
    protected caseDataService: TcCaseDataService;
    /**
     * The case reference
     */
    caseRef: string;
    /**
     * The LA Application Id
     */
    appId: string;
    /**
     * The LA Application Type Id (generally 1)
     */
    typeId: string;
    /**
     * sandboxId - this comes from claims resolver
     */
    sandboxId: number;
    /**
     * The Application ID of the UI (should ideally be unique as it is shared state key)
     */
    uiAppId: string;
    /**
     * Whether to show the header bar in the widget - eg. favorites on home page (contains icon etc) - if off icons still appear without bar
     */
    showHeader: boolean;
    /**
     * Layout object that can be passed to override default layout of the form renderer
     */
    layout: any[];
    /**
     * Allow override of forms framework
     * Options: bootstrap-4 or material-design
     */
    formsFramework: string;
    /**
     * Custom Form configuration file
     */
    customFormDefs: CustomFormDefs;
    /**
     * not used
     */
    customDataId: any;
    /**
     * Emit event to cause refresh of page
     * **/
    refreshEvent: EventEmitter<{}>;
    casedata: any;
    protected casedata$: ReplaySubject<any>;
    protected summary$: ReplaySubject<any>;
    protected metadata$: ReplaySubject<Metadata>;
    protected schema$: ReplaySubject<JsonSchema>;
    summary: any;
    metadata: Metadata;
    errorMessage: string;
    schema: JsonSchema;
    formRef: string;
    name: string;
    constructor(caseDataService: TcCaseDataService);
    triggerRefresh: () => void;
    refresh: () => void;
    ngOnInit(): void;
}

import { OnInit } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { MatSnackBar, MatDialog } from '@angular/material';
import { RoleAttribute, TcGeneralLandingPageConfigService } from '@tibco-tcstk/tc-core-lib';
import { TcDocumentService } from '../../services/tc-document.service';
import { LandingPageConfig } from '../../models/tc-live-apps-landing-page-config';
/**
 * Configuration page home
 *
 *@example <tcla-live-apps-setting-landing></tcla-live-apps-setting-landing>
 */
export declare class LiveAppsSettingsLandingComponent implements OnInit {
    private route;
    private generalLandingPageConfigService;
    private snackBar;
    private dialog;
    private documentsService;
    private landingPagesConfig;
    private sandboxId;
    private uiAppId;
    landingPages: LandingPageConfig[];
    selectedWelcomePage: LandingPageConfig;
    allRoles: RoleAttribute[];
    selectedRole: RoleAttribute[];
    constructor(route: ActivatedRoute, generalLandingPageConfigService: TcGeneralLandingPageConfigService, snackBar: MatSnackBar, dialog: MatDialog, documentsService: TcDocumentService);
    /**
    * @ignore
    */
    ngOnInit(): void;
    /**
     * Save Configuration
     */
    runSaveFunction(): void;
    /**
     * Delete Configuration
     */
    runDeleteConfiguration(): void;
    /**
     * New Configuration
     */
    runNewConfiguration: () => void;
    /**
     * Helper to Compare Objects
     */
    compareObjects: (o1: string, o2: string) => boolean;
    openDialog(imageFormat: string, location: string, index?: number): void;
    uploadFile(file: File, location: string, index?: number): void;
}

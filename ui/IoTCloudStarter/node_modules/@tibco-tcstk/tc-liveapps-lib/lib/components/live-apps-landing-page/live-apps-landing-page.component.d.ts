import { OnInit } from '@angular/core';
import { Location } from '@angular/common';
import { LandingPageItemConfig, TcGeneralLandingPageService } from '@tibco-tcstk/tc-core-lib';
import { Router, ActivatedRoute } from '@angular/router';
import { TcRolesService } from '../../services/tc-roles-service.ts.service';
/**
 * Landing page
 *
 * This component allows a configurable landing page on a per role basis
 *
 * @example <tcla-live-apps-landing-page></tcla-live-apps-landing-page>
 */
export declare class LiveAppsLandingPageComponent implements OnInit {
    private location;
    private route;
    private router;
    private landingPageService;
    private rolesService;
    title: string;
    subtitle: string;
    backgroundImage: string;
    highlights: LandingPageItemConfig[];
    private navigateURL;
    constructor(location: Location, route: ActivatedRoute, router: Router, landingPageService: TcGeneralLandingPageService, rolesService: TcRolesService);
    ngOnInit(): void;
    moveHome: () => void;
}

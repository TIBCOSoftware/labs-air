import { HttpClient } from '@angular/common/http';
import { CaseAction, CaseActionsList, CaseCreator, CaseCreatorsList } from '../models/liveappsdata';
import { LaProcessSelection } from '../models/tc-case-processes';
import { LiveAppsService } from './live-apps.service';
import { Observable } from 'rxjs';
import { TcCaseDataService } from './tc-case-data.service';
export declare class TcCaseProcessesService {
    private http;
    private liveAppsService;
    private caseDataService;
    constructor(http: HttpClient, liveAppsService: LiveAppsService, caseDataService: TcCaseDataService);
    getCaseActionsForCaseRef(caseRef: string, sandboxId: number, appId: string, typeId: string): Observable<CaseActionsList>;
    getCaseActions(caseRef: string, sandboxId: number, appId: string, typeId: string, caseState: string): Observable<CaseActionsList>;
    getCaseCreators(sandboxId: number, appId: string, typeId: string): Observable<CaseCreatorsList>;
    private getCaseIDAttributeName;
    private createLaProcessSelection;
    getProcessDetails(caseRef: string, appId: string, typeId: string, sandboxId: number, action: CaseAction, creator: CaseCreator, top: number): Observable<LaProcessSelection>;
}

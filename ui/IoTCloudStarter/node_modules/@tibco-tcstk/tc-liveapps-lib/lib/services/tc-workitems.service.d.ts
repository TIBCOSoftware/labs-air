import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { Workitem } from '../models/tc-workitems';
export declare class TcWorkitemsService {
    private http;
    constructor(http: HttpClient);
    getWorkitems(sandboxId: number, appIds: string[], caseRef: string, skip: number, top: number): Observable<Workitem[]>;
}

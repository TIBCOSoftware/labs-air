import { RouteAccessControlConfig } from '../models/tc-groups-data';
import { TcSharedStateService } from '@tibco-tcstk/tc-core-lib';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { Location } from '@angular/common';
export declare class TcAccessControlService {
    private http;
    private location;
    private sharedStateService;
    constructor(http: HttpClient, location: Location, sharedStateService: TcSharedStateService);
    createLAccessControlConfig(sandboxId: number, uiAppId: string, accessControlConfig: RouteAccessControlConfig): Observable<string>;
    getAccessControlConfig(uiAppId: string, useCache: boolean, flushCache: boolean): Observable<RouteAccessControlConfig>;
    updateAccessControlConfig(sandboxId: number, uiAppId: string, accessControlConfig: RouteAccessControlConfig, id: string): Observable<RouteAccessControlConfig>;
}

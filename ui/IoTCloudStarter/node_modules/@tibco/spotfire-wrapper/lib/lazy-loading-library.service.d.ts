import { Observable } from 'rxjs';
export declare class LazyLoadingLibraryService {
    private readonly document;
    private loadedLibraries;
    constructor(document: any);
    loadJs(url: string): Observable<any>;
}
